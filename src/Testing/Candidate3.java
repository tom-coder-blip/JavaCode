package Testing;

public class Candidate3 {
    public static void main(String[] args) {
        // Uncomment the array version below to make this work
        // int[] nums = {1, 2, 3, 4, 5};
        // String output = "";

        // for (int i = 0; i < nums.length; i++)
        //     output += nums[i] + " ";

        // System.out.println("Candidate 3 Output: " + output.trim());

        // Original version (will not compile):
        /*
        List<Integer> nums = Arrays.asList(1, 2, 3, 4, 5);
        String output = "";

        for (int i = 0; i < nums.length; i++)
            output += nums.get(i) + " ";

        System.out.println("Candidate 3 Output: " + output.trim());
        */
    }
}